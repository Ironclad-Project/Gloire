name=ed
from_source=ed
revision=1
hostdeps="gcc autoconf automake libtool pkg-config"
deps="core-libs mpfr gmp readline"

build() {
    cp -rp "${source_dir}"/. ./

    CC="x86_64-ironclad-gcc" ./configure

    make -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install

    post_package_strip
}
