name=neofetch
from_source=neofetch
revision=1
rundeps="bash"

configure() {	
    true
}

build() {
    true
}

install() {
    mkdir -p "${dest_dir}"/usr/bin
    cp -f "${source_dir}"/neofetch "${dest_dir}"/usr/bin/neofetch
}
