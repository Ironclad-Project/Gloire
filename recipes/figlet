name=figlet
from_source=figlet
revision=1
hostdeps="gcc pkg-config"
deps="core-libs"

build() {
    cp -rp "${source_dir}"/. ./

    make CC=${OS_TRIPLET}-gcc CFLAGS="${CFLAGS}" LD=${OS_TRIPLET}-gcc LDFLAGS="${CFLAGS}" prefix="${prefix}" -j${parallelism}
}

package() {
    DESTDIR="${dest_dir}" make install prefix="${prefix}"

    post_package_strip
}
