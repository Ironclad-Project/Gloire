name=ironclad
from_source=ironclad
revision=1
imagedeps="gcc-ada texinfo"
hostdeps="gprbuild"

configure() {
    "${source_dir}"/configure.sh --skip-menu
}

build() {
    gprbuild -j${parallelism}
}

install() {
    PREFIX=${prefix} DESTDIR=${dest_dir} ./install.sh
}
