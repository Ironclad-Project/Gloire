diff --git dconf-clean/meson.build dconf-workdir/meson.build
index 997149a..20c1735 100644
--- dconf-clean/meson.build
+++ dconf-workdir/meson.build
@@ -45,8 +45,10 @@ gio_dep = dependency('gio-2.0', version: gio_req_version)
 gio_unix_dep = dependency('gio-unix-2.0', version: gio_req_version)
 glib_dep = dependency('glib-2.0', version: '>= 2.67.4')
 
-gio_module_dir = gio_dep.get_variable(pkgconfig: 'giomoduledir', pkgconfig_define: ['libdir', dconf_libdir])
-dbus_session_service_dir = dependency('dbus-1').get_variable(pkgconfig: 'session_bus_services_dir', pkgconfig_define: ['datadir', dconf_datadir])
+# pkg-config prefixes path variables with the sysroot when cross-compiling,
+# which is right for finding files but wrong for an install directory.
+gio_module_dir = join_paths(dconf_libdir, 'gio', 'modules')
+dbus_session_service_dir = join_paths(dconf_datadir, 'dbus-1', 'services')
 
 systemd_userunitdir = get_option('systemduserunitdir')
 
@@ -68,11 +70,7 @@ have_bash = bash.found() # For completion scripts
 enable_bash_completion = get_option('bash_completion')
 if enable_bash_completion
   bash_completion_dep = dependency('bash-completion')
-  completions_dir = bash_completion_dep.get_variable(
-    pkgconfig: 'completionsdir',
-    # bash-completion 2.10 changed the substitutions
-    pkgconfig_define: bash_completion_dep.version().version_compare('>= 2.10') ? ['datadir', dconf_datadir] : ['prefix', dconf_prefix],
-  )
+  completions_dir = join_paths(dconf_datadir, 'bash-completion', 'completions')
 endif
 
 configure_file(
diff --git dconf-clean/service/meson.build dconf-workdir/service/meson.build
index a2045e5..f6c9e32 100644
--- dconf-clean/service/meson.build
+++ dconf-workdir/service/meson.build
@@ -16,7 +16,7 @@ configure_file(
   input: systemd_service + '.in',
   output: systemd_service,
   configuration: service_conf,
-  install_dir: systemd_userunitdir,
+  
 )
 
 lib_sources = [
diff --git dconf-clean/shm/dconf-shm.c dconf-workdir/shm/dconf-shm.c
index dbde759..2fe9410 100644
--- dconf-clean/shm/dconf-shm.c
+++ dconf-workdir/shm/dconf-shm.c
@@ -49,6 +49,11 @@ dconf_shm_close (guint8 *shm)
 guint8 *
 dconf_shm_open (const gchar *name)
 {
+  /* Ironclad: file mmap not supported, return NULL.
+   * dconf handles this gracefully - clients re-read the database. */
+  (void)name;
+  return NULL;
+#if 0
   const gchar *shmdir;
   gchar *filename;
   void *memory;
@@ -98,6 +103,7 @@ dconf_shm_open (const gchar *name)
   close (fd);
 
   return memory;
+#endif
 }
 
 void
@@ -127,6 +133,10 @@ dconf_shm_flag (const gchar *name)
        */
       if (dconf_shm_pwrite (fd, "", 1, 1) == 1)
         {
+          /* Ironclad: file mmap not supported, use pwrite() instead. */
+          guint8 flag = 1;
+          dconf_shm_pwrite (fd, &flag, 1, 0);
+#if 0
           guint8 *shm;
 
           /* It would have been easier for us to do write(fd, "\1", 1);
@@ -144,6 +154,7 @@ dconf_shm_flag (const gchar *name)
           *shm = 1;
 
           munmap (shm, 1);
+#endif
         }
 
       close (fd);
