name=gcon
from_source=gcon
revision=1
hostdeps="gcc binutils"
deps="mlibc"

configure() {
    "${source_dir}"/configure --prefix="${prefix}" --host=x86_64-ironclad
}

build() {
    make -j${parallelism}
}

install() {
    make install DESTDIR="${dest_dir}"
}
